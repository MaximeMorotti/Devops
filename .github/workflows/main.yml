name: CI devops 2025
on:
  push:
    branches: ["main", "develop"]
  pull_request:

jobs:
  # -------------------------------------------------------------
  # 1. ÉTAPE D'EXÉCUTION DES TESTS BACKEND
  # -------------------------------------------------------------
  test-backend: 
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Run Backend Tests
        run: mvn -B clean verify --file TP01/backend/pom.xml

      # On sauvegarde le dossier 'target' (contient les rapports de tests de couverture Jacoco)
      - name: Share test reports as artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend-target-dir
          path: TP01/backend/target/

  # -------------------------------------------------------------
  # 2. ÉTAPE D'ANALYSE SONAR (Séparée et dépendante des tests)
  # -------------------------------------------------------------
  sonar-analysis:
    needs: test-backend # Attend que les tests soient réussis
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Historique Git complet pour Sonar

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      # On télécharge le dossier 'target' généré par le job précédent
      - name: Download test reports
        uses: actions/download-artifact@v4
        with:
          name: backend-target-dir
          path: TP01/backend/target/

      - name: Run SonarCloud Scanner
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: >
          mvn sonar:sonar 
          -Dsonar.projectKey=MaximeMorotti_Devops
          -Dsonar.organization=maximemorotti 
          -Dsonar.host.url=https://sonarcloud.io 
          -Dsonar.login=${{ secrets.SONAR_TOKEN }}
          --file TP01/backend/pom.xml

  # -------------------------------------------------------------
  # 3. ÉTAPES CD : BUILD & PUSH (Attendent la validation de Sonar)
  # -------------------------------------------------------------
  build-and-push-backend:
    needs: sonar-analysis # Ne se lance QUE si Sonar donne son feu vert (Passed)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}
      - name: Build and push Backend
        uses: docker/build-push-action@v6
        with:
          context: ./TP01/backend 
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/my-backend:latest
          push: ${{ github.ref == 'refs/heads/main' }}

  build-and-push-database:
    needs: sonar-analysis
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}
      - name: Build and push Database
        uses: docker/build-push-action@v6
        with:
          context: ./TP01/database 
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/my-database:latest
          push: ${{ github.ref == 'refs/heads/main' }}

  build-and-push-httpd:
    needs: sonar-analysis
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}
      - name: Build and push HTTPD
        uses: docker/build-push-action@v6
        with:
          context: ./TP01/httpd    
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/my-httpd:latest
          push: ${{ github.ref == 'refs/heads/main' }}