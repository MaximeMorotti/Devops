- name: Launch Proxy Container
  docker_container:
    name: my-proxy-container
    image: mmorotti/my-httpd:latest
    ports:
      - "80:80"
    networks:
      - name: app-network
    restart_policy: always
  vars:
    ansible_python_interpreter: /opt/docker_venv/bin/python