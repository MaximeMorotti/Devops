all:
  vars:
    ansible_user: admin
    # C'est ici que ça doit correspondre au nom de fichier créé par le pipeline
    ansible_ssh_private_key_file: ~/.ssh/id_rsa 
  children:
    prod:
      hosts:
        maxime.morotti.takima.school: